coverage:
  status:
    project:
      default:
        target: 75%      # project-wide goal
        threshold: 1%    # allow small fluctuations
    patch:
      default:
        target: 75%
comment:
  layout: "reach, diff, files"
  behavior: default
ignore:
  - "docs/**"
  - "examples/**"
  - "tests/**"
